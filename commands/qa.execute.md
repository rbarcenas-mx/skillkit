---
description: Execute a QA validation plan locally — runs Docker, migrations, tests, HTTP flows, stress tests with progress bar, checkpoints, and error recovery. Usage: /qa.execute
---

Load the `qa.execute` skill via the `skill` tool and execute the steps defined in its SKILL.md.

No arguments required.
