---
description: Reads a ci/{id}_tasks.md plan and executes tasks in order with save points, live progress bar, rollback on failure, and resume capability. 100% local via run.py. Usage: /ci.execute [plan-id]
---

Load the `ci.execute` skill via the `skill` tool and execute the steps defined in its SKILL.md.

**Arguments**: `$ARGUMENTS` — optional plan ID or path to task file.
