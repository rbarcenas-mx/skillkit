---
description: Generate Mermaid.js architecture diagrams from spec-kit artifacts. Detects project type, builds manifest, generates diagrams one at a time. Model resolved via TOKEN_BUDGET. Usage: /speckit.diagrams <feature-name>
---

Load the `speckit.diagrams` skill via the `skill` tool and execute the steps defined in its SKILL.md.

**Arguments**: `$ARGUMENTS` — feature name (e.g. `001-my-feature`). If empty, prompts user.
