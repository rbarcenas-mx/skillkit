---
description: Review pull requests with structured analysis: blast radius, security, breaking changes, performance, tests, and code quality. Supports batching for large diffs. Usage: /pr-review-expert <pr-number>
---

Load the `pr-review-expert` skill via the `skill` tool and execute the steps defined in its SKILL.md.

**Arguments**: `$ARGUMENTS` — PR number (required). For GitLab MRs, pass MR IID.
