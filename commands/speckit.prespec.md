---
description: Analyzes a raw development idea in two phases — detects ambiguities, contradictions, missing scope, and generates MVP-critical questions. Refines with user decisions. Output: idea.md. Usage: /speckit.prespec [path-to-idea]
---

Load the `speckit.prespec` skill via the `skill` tool and execute the steps defined in its SKILL.md.

**Arguments**: `$ARGUMENTS` — optional path to idea file. Defaults to `idea.txt` in working directory.
