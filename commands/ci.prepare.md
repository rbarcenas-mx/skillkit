---
description: Analyze the current project state and generate a CI integration plan to GitHub with atomic commits, rollback commands, and save points. Saves plan to ci/{id}_tasks.md. Usage: /ci.prepare
---

Load the `ci.prepare` skill via the `skill` tool and execute the steps defined in its SKILL.md.

No arguments required.
