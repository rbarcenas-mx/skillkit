---
description: Resolve audit findings post-speckit.audit. Generates audit/{id}-audit-resolve.md with resolved findings and solution timestamp. Usage: /speckit.audit-resolve
---

Load the `speckit.audit-resolve` skill via the `skill` tool and execute the steps defined in its SKILL.md.

No arguments required.
