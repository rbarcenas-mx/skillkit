---
description: Validate (lint, build, tests), review, push to GitHub, and monitor CI for commits from ci.execute. Solo and team mode. Usage: /ci.ship
---

Load the `ci.ship` skill via the `skill` tool and execute the steps defined in its SKILL.md.

No arguments required.
