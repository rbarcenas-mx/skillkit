---
description: Progressive audit of specs, plans, tasks, or code. Detects inconsistencies, ambiguities, and risks. Saves results to audit.md. Usage: /speckit.audit [optional-stage]
---

Load the `speckit.audit` skill via the `skill` tool and execute the steps defined in its SKILL.md.

**Arguments**: `$ARGUMENTS` — optional stage filter (spec/plan/tasks/codigo/lint).
