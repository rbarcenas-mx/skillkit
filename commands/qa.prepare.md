---
description: Analyze the project and generate QA validation plans by type (infrastructure, unit, HTTP flow, stress, scaling). Generates plans in qa/{id}_plan.md. Usage: /qa.prepare
---

Load the `qa.prepare` skill via the `skill` tool and execute the steps defined in its SKILL.md.

No arguments required.
